import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["plus.unsplash.com", "offermart.s3.us-west-1.amazonaws.com"],
  },
};

export default nextConfig;
